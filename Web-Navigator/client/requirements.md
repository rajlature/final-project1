## Packages
leaflet | Maps visualization
react-leaflet | React components for Leaflet maps
recharts | Data visualization charts for traffic stats
framer-motion | Smooth page transitions and UI animations
date-fns | Date formatting
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
API uses multipart/form-data for video uploads.
Leaflet requires CSS import in main entry or component.
